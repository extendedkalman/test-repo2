name: use it

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo
      uses: actions/checkout@v2

    - name: action usage
      uses: extendedkalman/test-repo@v4
      with:
        message: "Merhaba, Dünya!"
